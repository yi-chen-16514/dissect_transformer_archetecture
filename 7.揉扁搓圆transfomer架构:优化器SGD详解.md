前面我们讨论反向传播算法的原理，本质上是通过链式求导，计算网络中每个参数的梯度，然后随着梯度相反方向来修改参数的值，最终达到修改后通过网络构造的损失函数计算取值变小的目的。现在的情况是如何执行“随着梯度相反方向来修改参数的值”这个步骤。这也是深度学习算法中优化器部分的来源。

在所有优化器算法中最基础的就是SGD，它采取的原则最简单。那就是在计算出给定参数求导处的数值后，将数值乘以一个预先设定好的参数，也叫学习率，然后将参数值减去求导值乘以学习率的结果。从数学角度看就是:

<img width="508" height="82" alt="ec5640ea24e789137a33cb9bcdc8fdb8" src="https://github.com/user-attachments/assets/db265172-a396-429d-a0eb-b432b21e5065" />


上面公式的意思是，我们先拿一批训练数据，这批数据对应输入:x_i和结果:y_i。然后把x_i输入给模型获得模型的预测结果。接着把预测结果跟预定结果比较，计算模型预测结果与正确结果的差距，这里就是我们前面所说的损失函数，也就是航母公式中右边的拉丁字母l所表示。由于我们计算损失是基于一批训练数据的基础上，假设一批训练数据有10个记录，那么我们分别计算模型对这是个记录的预测数值，将预测数值与预定数值放入损失函数计算预测差距，然后求和，最后再求平均值，这就是深度训练中所谓的min-batch方法。

然后我们根据上面的损失函数，针对模型里面每个参数做链式求导，算出每个参数对应的导致值，将这个数值乘以一个预先设置好的学习率，接着将当前参数值减去前面计算的结果就得到参数调整后的数值:

<img width="344" height="78" alt="947ad2e3a64ad04035c577c2255862a2" src="https://github.com/user-attachments/assets/19192dc4-48e7-4102-89ac-ed4ba8bf7376" />

这种方法简单但实用，当然也存在着自身的局限性，后续很多优化器算法都基于该方法扩展或延伸而来。下面我们通过一个简单的示例结合代码来理解SGD算法的原理。首先我们设定一个简单的线性回归模型y=wx+b，然后损失函数我们使用均方差MSE，为了简单我们将一批训练数据的量简化为1，也就是前面损失函数中的N设置为1。接着我们设定w的真实数值为2，b的真实数值为1，当然我们预先并不知道这些数值结果，我们通过一系列训练数据，结合SGD算法，看看如何逼近w,b的真实数值。

首先我们先创建100个用于训练的数据点:

<img width="262" height="60" alt="9b52e13f6f4c144795b43c19e6836331" src="https://github.com/user-attachments/assets/bc94085b-6819-4e03-b995-ae4c8c2f884c" />

上面公式末尾参数epsilon用于模拟真实世界获取数据时比如会参入噪音而得不到完全准确的真实数据。例如给你一根10厘米的毛线，然后给你一把尺子也测量它的长度。在绝大多数情况下你量出来的数值都不是准确的10厘米，而更可能是9.8厘米这样的数据。这是因为毛线可能没有被完全捋直，它在某个位置有微弱的弯曲而我们没有发现，或者我们测量时尺子摆放有难以察觉的偏离，没有完全跟毛线平行等。这个epsilon噪音我们选定为均值为0，方差为0.5的正太分布的随机变量。

在前面章节中我们看到，模型在一开始的时候会将其内部参数设定为给定的初始值，这个初始值非常重要，它关系到模型后期训练是否能正确收敛，以前很多开原模型都不会给出这些初始值，但deepseek就毫不保留得全给了。在这里我们先设定w,b的初始值为3.0,0.5。由于参数值跟准确的数值不同，因此模型根据训练数据给出的结果必然跟预定的真实结果有差异，我们的任务是结合差异数值，在SGD优化器算法的支持下不断修改w,b的数值从而不断逼近其真实数值。

假定我们根据真实模型获得的一个训练数据为x_i, y_i, 然后我们把x_i放入到模型中得到的预测结果就是y'_i = w*x_i+b,MSE的损失计算方式就是将模型预测的结果与真实结果求差值的平方，也就是l_i=(y_i-y'_i)^2, 为了计算上的方便，我们做一个线性转换: l_i=1/2*(y_i-y'_i)^2,这里我们增加一个常量系数不会改变损失函数对模型预测与真实结果的差异的评估，也就是不会影响训练结果，这里添加的参数主要是想求导时，把右上角的数值2给取消掉以方便计算。

有了损失函数后，我们就能基于损失函数的基础上对两个参数分别求导:

<img width="524" height="128" alt="da5c0b00c612b1c6f1c0cd7025e432a5" src="https://github.com/user-attachments/assets/76c5b04e-7ca3-43de-a239-f5836b5f523d" />

<img width="478" height="126" alt="12759a899ae54877e02d149581932f59" src="https://github.com/user-attachments/assets/e188f66b-4afe-4ba1-b0ef-300d3a1c28e3" />

然后根据上面计算的求导结果分别乘以预先设定好的学习率，再将当前的参数值减去前面计算的结果得到参数调整后的结果:

<img width="368" height="236" alt="3441538c969d62f5753311c564a82ccd" src="https://github.com/user-attachments/assets/c0a9108d-8a2c-482b-8e12-00f910862d34" />

下面我们使用代码将上面的过程实现出来以便帮我们更好的理解算法流程:
```py
import numpy as np
import matplotlib.pyplot as plt

# 设置随机种子，保证可复现
np.random.seed(42)

# -------------------- 1. 生成数据 --------------------
n_samples = 100
true_w = 2.0
true_b = 1.0
x = np.random.uniform(0, 10, n_samples)
noise = np.random.normal(0, 0.5, n_samples)
y = true_w * x + true_b + noise

# -------------------- 2. 初始化参数 --------------------
w = 3.0
b = 0.5
learning_rate = 0.01
epochs = 50

# 存储历史数据用于绘图
history = {'epoch': [], 'loss': [], 'w': [], 'b': []}

print("初始参数: w = {:.4f}, b = {:.4f}".format(w, b))
print("开始 SGD 训练...")
print("="*60)

for epoch in range(1, epochs + 1):
    # 打乱数据顺序
    indices = np.random.permutation(n_samples)
    x_shuffled = x[indices]
    y_shuffled = y[indices]
    
    # 遍历每个样本进行更新
    for i in range(n_samples):
        xi = x_shuffled[i]
        yi = y_shuffled[i]
        # 预测
        y_pred = w * xi + b
        # 误差
        error = yi - y_pred
        # 梯度
        grad_w = -error * xi
        grad_b = -error
        # 更新
        w -= learning_rate * grad_w
        b -= learning_rate * grad_b
    
    # 计算当前所有样本的平均损失 (MSE)
    y_pred_all = w * x + b
    loss = np.mean((y - y_pred_all) ** 2)
    
    # 记录历史
    history['epoch'].append(epoch)
    history['loss'].append(loss)
    history['w'].append(w)
    history['b'].append(b)
    
    # 每5个epoch打印一次详细信息
    if epoch % 5 == 0 or epoch == 1:
        print("Epoch {:3d} | Loss = {:.6f} | w = {:.4f} | b = {:.4f}".format(
            epoch, loss, w, b))

print("="*60)
print("最终参数: w = {:.4f}, b = {:.4f}".format(w, b))
print("真实参数: w = {:.4f}, b = {:.4f}".format(true_w, true_b))

# -------------------- 绘制损失下降曲线 --------------------
plt.figure(figsize=(12, 4))

plt.subplot(1, 2, 1)
plt.plot(history['epoch'], history['loss'], 'b-')
plt.xlabel('Epoch')
plt.ylabel('MSE Loss')
plt.title('Loss下降曲线')
plt.grid(True)

plt.subplot(1, 2, 2)
plt.plot(history['epoch'], history['w'], 'r-', label='w')
plt.plot(history['epoch'], history['b'], 'g-', label='b')
plt.axhline(y=true_w, color='r', linestyle='--', label='True w')
plt.axhline(y=true_b, color='g', linestyle='--', label='True b')
plt.xlabel('Epoch')
plt.ylabel('参数值')
plt.title('参数收敛过程')
plt.legend()
plt.grid(True)

plt.tight_layout()
plt.show()
```
上面代码执行后所得结果如下:
```py
初始参数: w = 3.0000, b = 0.5000
开始 SGD 训练...
============================================================
Epoch   1 | Loss = 0.284053 | w = 2.0609 | b = 0.5691
Epoch   5 | Loss = 0.212269 | w = 1.9975 | b = 0.9278
Epoch  10 | Loss = 0.202971 | w = 1.9890 | b = 1.0595
Epoch  15 | Loss = 0.205640 | w = 1.9913 | b = 1.0877
Epoch  20 | Loss = 0.291500 | w = 2.0306 | b = 1.1103
Epoch  25 | Loss = 0.201668 | w = 1.9766 | b = 1.1140
Epoch  30 | Loss = 0.202610 | w = 1.9741 | b = 1.0916
Epoch  35 | Loss = 0.250693 | w = 2.0164 | b = 1.1109
Epoch  40 | Loss = 0.416256 | w = 1.8922 | b = 1.1172
Epoch  45 | Loss = 0.424368 | w = 2.0585 | b = 1.1300
Epoch  50 | Loss = 0.217275 | w = 1.9552 | b = 1.1029
============================================================
最终参数: w = 1.9552, b = 1.1029
真实参数: w = 2.0000, b = 1.0000
```
上面输出中，一个epoch指的是循环执行的次数，这个次数也是我们预先指定。在大模型训练中通常不会预先指定循环训练的次数，而是一直运行直到达到某种效果为止。从代码中我们看到，每次我们先获取一条训练数据。然后使用训练数据的x数值放入到模型计算预测的y'_i，然后使用均方差计算y'_i与y_i的差距，在此基础上分别对两个参数求导，将求导结果乘以学习率，然后分别对w,b减去求导结果乘以学习率的结果从而更新当前参数。从上面的结果输出可以看到，每一个epoch执行后，参数的调整就越来越接近真实的参数值，最后我们得到的w=1.9552,b=1.1029已经非常接近真实的w=2,b=1.以下就是训练过程中参数变化相对于真实值的情况:

<img width="1189" height="390" alt="0ffb1fa31b5cabe2b634cb3b46d56a52" src="https://github.com/user-attachments/assets/d200d606-2417-47db-8a52-50cb26af2441" />

从上面图像可以看到红色实线对应的就是参数的调整过程，红色虚线对应真实的w数值，绿色实线对应参数b的变化，绿色虚线对应真实的参数b的数值，从图像可以看到，两个参数的调整不断的解决真实数值。SGD优化算法本身也存在严重的缺陷，它产生的梯度数值噪声大导致收敛不稳定，对于复杂的大模型它基本不适用。其次它非常依赖于预先设定好的学习率，这个设置设置不合适，训练就不会让参数逼近真实数值。如果模型对应的真实数学函数复杂，那么SGD可能收敛到鞍点，或者一直反复震旦一直走不到最优点。

下面我们看SGD无法将参数优化到真实值的情况:
```py
import numpy as np
import matplotlib.pyplot as plt

# 定义函数和梯度
def f(x):
    return (x**2 - 4)**2 + 0.1*x

def grad_f(x):
    return 4*x*(x**2 - 4) + 0.1

# 梯度下降（模拟SGD）
def sgd(start, lr=0.01, epochs=100):
    x = start
    path = [x]
    for _ in range(epochs):
        x = x - lr * grad_f(x)
        path.append(x)
    return np.array(path)

# 从两个不同起点出发
start1 = 0.0   # 会收敛到全局最小
start2 = 2.5   # 会收敛到局部最小

path1 = sgd(start1, lr=0.01, epochs=150)
path2 = sgd(start2, lr=0.01, epochs=150)

final1 = path1[-1]
final2 = path2[-1]

print(f"从 x={start1} 出发，最终到达 x={final1:.4f}, f={f(final1):.4f}")
print(f"从 x={start2} 出发，最终到达 x={final2:.4f}, f={f(final2):.4f}")
print(f"全局最小约在 x=-1.99, f=-0.197")
print(f"局部最小约在 x= 1.98, f= 0.204")

# 绘制函数曲线和轨迹
x_vals = np.linspace(-3, 3, 500)
y_vals = f(x_vals)

plt.figure(figsize=(10, 6))
plt.plot(x_vals, y_vals, 'b-', linewidth=2, label='f(x)')

# 绘制两个轨迹
plt.plot(path1, f(path1), 'ro-', markersize=4, label=f'起点 x={start1}')
plt.plot(path2, f(path2), 'go-', markersize=4, label=f'起点 x={start2}')

# 标记全局最小和局部最小
plt.axvline(-1.99, color='r', linestyle='--', alpha=0.6, label='全局最小')
plt.axvline(1.98, color='g', linestyle='--', alpha=0.6, label='局部最小')

plt.xlabel('x')
plt.ylabel('f(x)')
plt.title('SGD 在一维非凸函数上陷入局部最优')
plt.legend()
plt.grid(True)
plt.show()
```
上面代码我们构造了目标函数: f(x)=(x^2 - 4)^2 + 0.1x，我们的目标是如何逼近x的真实值，使得x带入到f后能取得函数的最小值。能够明显看到，x=-2是能让f(x)取得最小值，但是x=2能够让f在局部取得最小值，面对这样的函数，sgd有可能被x=2困住而找到不x=-2,上面的代码展示的正是这点，运行上面代码我们得到如下结果:

从 x=0.0 出发，最终到达 x=-2.0031, f=-0.2002
从 x=2.5 出发，最终到达 x=1.9969, f=0.1998
全局最小约在 x=-1.99, f=-0.197
局部最小约在 x= 1.98, f= 0.204

这意味着当我们初始化x的值为0.0时，sgd算法能找到让x逼近目标值也就是-2,但是如果我们初始化x参数一开始为2.5时，那么sgd算法就会一直困在x=1.9969处，也就是无论运行多久sgd算法都无法抵达目标数值-2，上面代码还会画出如下的运行效果:

<img width="841" height="547" alt="daf61da78ab22c8b7d9ed02b99cc2ee6" src="https://github.com/user-attachments/assets/bff9e2a4-0d0a-4a60-bab1-256091e26cc9" />


从上图看到，如果初始化的数值在右边绿色点，那么sgd算法就会落入到左边的最低点，如果初始化的数值为左边红色点，也就是初始化取值为0时，sgd算法就能走到左边的最低点，也就是能让x逼近目标值-2.由于sgd算法具备这样的缺陷，我们才会有后面一系列Adam等新的优化算法
