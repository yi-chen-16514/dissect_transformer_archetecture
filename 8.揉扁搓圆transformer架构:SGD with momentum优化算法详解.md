前面我们研究了sgd优化算法。这个算法的逻辑很简单，先在当前点求到，根据导数的数值决定“往下”的方向。如果当前数据点的导数取值为正，那么向x轴的右边就是“向上”的方向，于是反向也就是向"左边"就是向下的方向。
我们的目标是尽可能往下走直到最低点为止。于是sgd的算法就是获得当前数据点对应的导数值，其实也就是当前点的“坡度”,乘以一个预先设置好的学习率,然后减去学习率与导数值的乘积，由此获得当前参数更新后的数值。

SGD算法存在一个问题是，当我们已经抵达一个局部最低点时，它将不再产生更新效果。了解微积分就知道，在局部最小值点求导的结果是0，这意味着使用学习率乘以当前点的导数值也就是0，结果还是0，因此sgd就无法再根据
这个乘积结果去更新参数，毕竟乘积结果数值时0.于是人们就在这个算法上做了改进，那就是给sgd增加一个“惯性”。想象一个情形，你飞快的骑着自行车前进，如果前面有一个小水坑，你在抵达水坑前停止登踏，那么接下来出现
的情况是，自行车冲入水坑，你在车上会感觉一股震旦，然后自行车依靠前进的惯性冲出水坑，然后继续前进。

SGD with momentum算法就是依靠这个原理来冲出局部最小“坑”。它的基本思想是在每一步参数更新时积累前进的惯性。这就好像你骑自行车时，每登踏一次，自行车就积累了一些前进的惯性。如果你前面登踏的次数多，那么
当你停止登踏后，自行车就会依靠惯性走的更远，如果你前面登踏的次数少，那么惯性小，自行车在你停止登踏后依然会继续前进，只不过前进的距离就会小。这里需要注意，无论你在停止登踏前多用力的踩踏踏板，自行车都会在
失去动力后自行前进一段距离后停止，它不会在失去动力后无止境的保持前行，那是因为有空气阻力的存在，惯性会在反向空气阻力的影响下逐渐衰减为0，SGD with momentum算法也将阻力考虑了进去，我们先看看其对应的数学公式:

<img width="378" height="200" alt="03fce60cab38a7e0ce6358a5d2a41b4c" src="https://github.com/user-attachments/assets/e84dc15b-71fe-469e-96bb-985b8394746c" />

在上面公式中，g_t对应的就是针对当前参数求导后的导数值；beta对应的就是惯性衰减系数，这里模拟的就是我们上面说的空气阻力，这个系数由我们预先设定，它取值在[0,1)之间，通常取值0.9；v就是惯性，它是一个迭代出来的值。
可以看到v_(t+1)的计算除了向前面sgd那样使用学习率乘以当前参数的导数值，同时它还加上了以前积累的惯性速度，这意味着当我们进入到局部最小值点，g_t也就是当前参数求导后取值为0时，我们依然还能依赖以前的惯性继续向前冲，
那么这个冲力就有可能把我们带出当前的局部最小值点，这就像我们骑车在水坑前面停止登踏后，尽管自行车失去了向前的动力，但是原有惯性依然能推动自行车继续前进从而冲入水坑后又继续冲出水坑。
